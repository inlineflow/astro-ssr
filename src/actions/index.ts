import { brand } from "./establishment";
import { appointment } from "./appointment";
import { location } from "./location";

export const server = {
  establishment: brand,
  appointment,
  location,
};
