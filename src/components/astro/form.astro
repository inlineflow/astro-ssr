---
import { AppointmentForm } from "../react/AppointmentForm";
import type { ServiceValidated } from "root/src/types";

interface Props {
  validService: ServiceValidated;
}

const { validService } = Astro.props;
---

{validService && <AppointmentForm service={validService} client:load />}
